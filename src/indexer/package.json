{
  "name": "indexer",
  "version": "1.0.0",
  "description": "Indexer scripts",
  "scripts": {
    "poetry": "poetry shell",
    "up": "docker compose up -d",
    "down": "docker compose down",
    "start": "dipdup -c local.dipdup.yml run",
    "wipe": "dipdup -c local.dipdup.yml schema wipe --force",
    "restart": "yarn wipe && yarn start",
    "import-contracts": "python ./scripts/import-contracts.py",
    "replicate-config": "python ./scripts/replicate-config.py"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
