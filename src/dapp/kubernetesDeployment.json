{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": { "name": "mavryk-frontend" },
  "spec": {
    "replicas": 3,
    "selector": { "matchLabels": { "app": "mavryk-frontend" } },
    "template": {
      "metadata": { "labels": { "app": "mavryk-frontend" } },
      "spec": {
        "terminationGracePeriodSeconds": 30,
        "containers": [
          {
            "name": "mavryk-frontend",
            "image": "registry.digitalocean.com/mavryk/mavryk-frontend",
            "imagePullPolicy": "Always"
          }
        ]
      }
    }
  }
}
