const {oracle0, oracle1, oracle2, oracle3, oracle4} = require('./accounts')

module.exports = {
    oracles: [
        oracle0,
        oracle1,
        oracle2,
        oracle3,
        oracle4,
    ]
}
