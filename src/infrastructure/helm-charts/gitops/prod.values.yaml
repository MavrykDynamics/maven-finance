spec:
  environment: prod
  valueFiles: prod.values.yaml
  clusterName: in-cluster

argoCD:
  valueFiles: network.values.yaml

certManager:
  valueFiles: network.values.yaml

externalDNS:
  valueFiles: network.values.yaml

kubePrometheusStack:
  valueFiles: network.values.yaml

sealedSecrets:
  valueFiles: network.values.yaml
