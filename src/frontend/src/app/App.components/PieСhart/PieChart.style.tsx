import styled, { keyframes } from 'styled-components/macro'

import { whiteColor } from '../../../styles'

export const PieChartWrap = styled.figure`
  margin: 0;
  width: 254px;
  height: 254px;
  border-radius: 50%;
  overflow: hidden;
`
