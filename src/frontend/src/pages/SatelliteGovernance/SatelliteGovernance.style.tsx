import styled from 'styled-components/macro'
import {
  cyanColor,
  containerColor,
  royalPurpleColor,
  downColor,
  headerColor,
  skyColor,
  subTextColor,
  infoColor,
  upColor,
} from 'styles'

export const SatelliteGovernanceStyled = styled.section`` // SatelliteGovernanceStyled
