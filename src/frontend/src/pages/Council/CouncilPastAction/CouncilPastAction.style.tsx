import styled from 'styled-components/macro'
import { Card, downColor, upColor, skyColor, headerColor, royalPurpleColor } from 'styles'

export const CouncilPastActionStyled = styled(Card)`
  margin: 0;
`
