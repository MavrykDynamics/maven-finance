import { StageThreeFormView } from './StageThreeForm.view'

type StageThreeFormProps = {
  id: number
  label: string
}
export const StageThreeForm = ({}: StageThreeFormProps) => {
  return <StageThreeFormView />
}
